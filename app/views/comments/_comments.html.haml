.div.container
  %hr
  - f.each do |item|
    %div.row.comments
      
      %div.col-md-1
        User id:
        =item.user_id
      %div.col-md-7
        =item.content
      %div.col-md-3
        =item.created_at
      %div.col-md-1
        - if current_user
          - if current_user.id == item.user_id
            %li= link_to 'Delete', comment_path(item.id), method: :delete
    %hr
      